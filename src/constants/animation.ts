/** Expo ease-out — used for entrance animations */
export const EASE_OUT_EXPO = [0.25, 0.46, 0.45, 0.94] as const;

/** ScrollReveal transition duration in seconds */
export const REVEAL_DURATION_S = 0.8;

/** Extra buffer (ms) after reveal transition before removing inline styles */
export const REVEAL_CLEANUP_MS = 100;
