/** Fixed navbar height in pixels (used for scroll offset calculations) */
export const NAV_HEIGHT = 72;

/** How close (px) a section top must be to the navbar to count as "active" */
export const SECTION_PROXIMITY_PX = 10;

/** Margin (px) from page bottom to consider the user "at the bottom" */
export const SCROLL_BOTTOM_MARGIN_PX = 2;

/** IntersectionObserver root margin for scroll-triggered reveals */
export const INVIEW_MARGIN = "-80px";
